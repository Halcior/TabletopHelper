type Json = any;
export interface SeedCoreReport {
    faction: string;
    detachmentsCreated: string[];
    stratagemsCreated: string[];
    enhancementsCreated: string[];
    skipped: {
        ability_id: string;
        type: string;
        reason: string;
    }[];
}
/**
 * Pure core: produce the new core entities for `missing` abilities, drawing
 * required facts from `facts`. Returns the records to append + a report; mutates
 * nothing.
 */
export declare function seedCore(faction: string, missing: {
    ability_id: string;
    ability_type: string;
    detachment_id: string | null;
    name: string;
}[], facts: Map<string, Json>, existing: {
    stratagems: Set<string>;
    enhancements: Set<string>;
    detachments: Set<string>;
}): {
    detachments: Json[];
    stratagems: Json[];
    enhancements: Json[];
    report: SeedCoreReport;
};
export {};
//# sourceMappingURL=author-seed-core.d.ts.map