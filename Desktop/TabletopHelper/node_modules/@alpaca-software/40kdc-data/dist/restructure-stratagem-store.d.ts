/** Split a stratagem blob into structured sections. Returns null if not parseable. */
export declare function parseStratagemBlob(raw: string): {
    when: string;
    target: string;
    effect: string;
    restrictions?: string;
} | null;
//# sourceMappingURL=restructure-stratagem-store.d.ts.map