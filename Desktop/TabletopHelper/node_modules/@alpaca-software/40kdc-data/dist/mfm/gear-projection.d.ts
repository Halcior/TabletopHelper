import { MfmDump, type WargearItemRow } from "./loader.js";
export interface GameVersion {
    edition: string;
    dataslate: string;
}
export interface WeaponProfile {
    name: string;
    range: number | "Melee";
    stats: {
        A: number | string;
        BS?: number | null;
        WS?: number | null;
        S: number | string;
        AP: number;
        D: number | string;
    };
    keywords?: {
        keyword_id: string;
        parameters?: Record<string, unknown>;
    }[];
}
export interface WeaponRecord {
    id: string;
    name: string;
    type: "ranged" | "melee";
    profiles: WeaponProfile[];
    game_version: GameVersion;
}
export interface WargearRecord {
    id: string;
    name: string;
    game_version: GameVersion;
    category?: string;
}
export interface MintContext {
    dump: MfmDump;
    gv: GameVersion;
    warnings: string[];
}
export declare function parseSkill(value: string | null | undefined): number | null;
/** Build a repo weapon record from one dump wargear item and its profiles. */
export declare function mintWeapon(ctx: MintContext, item: WargearItemRow, id: string, name: string): WeaponRecord;
/** Build a repo record for selectable non-weapon equipment. */
export declare function mintWargear(ctx: MintContext, item: WargearItemRow, id: string, name: string): WargearRecord;
//# sourceMappingURL=gear-projection.d.ts.map