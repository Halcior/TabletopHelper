import { MfmDump } from "./loader.js";
import type { StagedWrite } from "./apply.js";
export interface DirVariantResult {
    dir: string;
    conflictingIds: number;
    variantsAdded: number;
    unitsRewired: number;
    duplicatesDropped: number;
    warnings: string[];
}
export interface WeaponVariantsReport {
    dirs: DirVariantResult[];
    staged: StagedWrite[];
}
/** Build the variant-split projection for one or every faction dir. */
export declare function runWeaponVariants(dump: MfmDump, onlyDir?: string): WeaponVariantsReport;
/** Render the dry-run/applied report as markdown. */
export declare function buildWeaponVariantsReport(report: WeaponVariantsReport, write: boolean): string;
//# sourceMappingURL=weapon-variants.d.ts.map