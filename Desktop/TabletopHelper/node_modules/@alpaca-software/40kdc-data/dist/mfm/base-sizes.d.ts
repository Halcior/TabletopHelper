import { MfmDump } from "./loader.js";
import type { StagedWrite } from "./apply.js";
export interface BaseSize {
    shape: "round" | "oval" | "flying-base" | "hull" | "unique";
    diameter?: number;
    width?: number;
    length?: number;
    size?: "small" | "large";
    draft?: boolean;
}
/**
 * Parse a dump `baseSize` label to a repo base-size object — confident cases
 * only. Returns null for categories, per-model/multi strings, and ambiguous
 * single-number ovals (the caller keeps the authored value for those).
 */
export declare function parseBaseSize(raw: string | null | undefined): BaseSize | null;
/** Dimensional equality (shape + mm), ignoring the draft flag. */
export declare function baseSizeEqual(a: BaseSize | undefined, b: BaseSize): boolean;
/** dir → unitId → dump-derived base size (confident round/oval only). */
export declare function buildBaseSizeCanon(dump: MfmDump): Map<string, Map<string, BaseSize>>;
export interface BaseSizeReport {
    filled: {
        dir: string;
        id: string;
    }[];
    dedrafted: {
        dir: string;
        id: string;
    }[];
    corrected: {
        dir: string;
        id: string;
        from: string;
        to: string;
    }[];
    review: {
        dir: string;
        id: string;
        authored: string;
        dump: string;
    }[];
    confirmed: number;
    staged: StagedWrite[];
}
export declare function runBaseSizes(dump: MfmDump): BaseSizeReport;
export declare function buildBaseSizeReport(report: BaseSizeReport, write: boolean): string;
//# sourceMappingURL=base-sizes.d.ts.map