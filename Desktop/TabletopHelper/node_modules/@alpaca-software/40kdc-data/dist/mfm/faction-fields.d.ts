import { MfmDump } from "./loader.js";
import type { StagedWrite } from "./apply.js";
export interface DirFactionResult {
    dir: string;
    ruleFilled?: string;
    ruleConfirmed?: boolean;
    ruleReview?: {
        authored: string | null;
        candidates: string[];
    };
    parentFilled?: string;
    parentConfirmed?: boolean;
    parentReview?: {
        authored: string;
        derived: string;
    };
    aliasesAdded: string[];
}
export interface FactionFieldsReport {
    dirs: DirFactionResult[];
    /** Repo faction dirs with no matching dump faction keyword (left untouched). */
    unresolvedDirs: string[];
    staged: StagedWrite[];
}
export declare function runFactionFields(dump: MfmDump): FactionFieldsReport;
export declare function buildFactionFieldsReport(report: FactionFieldsReport, write: boolean): string;
//# sourceMappingURL=faction-fields.d.ts.map