/** Army points ceiling for a battle size; null when the size is unknown. */
export function pointsLimitForBattleSize(battleSize) {
    switch (battleSize) {
        case "strike-force":
            return 2000;
        case "incursion":
            return 1000;
        default:
            return null;
    }
}
/** 11e detachment-point budget for a battle size; null when the size is unknown. */
export function detachmentCapForBattleSize(battleSize) {
    switch (battleSize) {
        case "strike-force":
            return 3;
        case "incursion":
            return 2;
        default:
            return null;
    }
}
//# sourceMappingURL=battle-sizes.js.map